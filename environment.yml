name: term-matching-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - gensim
  - pandas
  - pip
  - pip:
    - torch
    - transformers
    - sentence-transformers
    #- spacy
    - scikit-learn
    - git-lfs